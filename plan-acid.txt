Стоп. Не добавляй новые темы и не продолжай делать страницы.

Сейчас нужно исправить архитектурную ошибку.

Проблема:
1. При выборе Acid фон и хедер становятся Acid, но остальные страницы частично остаются light/dark.
2. Если до Acid была light-тема — компоненты на Challenges/Create/etc остаются светлыми.
3. Если до Acid была dark-тема — компоненты остаются тёмными.
4. Значит Acid не управляет компонентами через tokens, а просто накладывается поверх старой light/dark системы.
5. В Header сломался layout: поиск, плюс и профиль переехали в центр.
6. На десктопе появилась лишняя hamburger-кнопка, которая дублирует меню профиля.

Нужно сделать cleanup, а не добавлять функционал.

Задача 1. Удалить временные Acid-specific компоненты

Удалить или перестать использовать:
- HeaderAcid
- FeedPageAcid
- любые новые PageNameAcid
- ap-* как основной слой UI

Не создавать:
- FriendsPageAcid
- ProfilePageAcid
- LeaderboardPageAcid
- ChallengesPageAcid
- HeaderRoyal / HeaderBlueprint и т.д.

Должны остаться:
- один Header.tsx
- один Layout.tsx
- одна FeedPage.tsx
- одна ChallengesPage.tsx
- одна SettingsPage.tsx
- одна FriendsPage.tsx

Задача 2. Header layout

Переделать Header.tsx на структуру:

<header className="cork-header">
  <div className="cork-header__inner">
    <Link className="cork-brand" />
    <nav className="cork-nav">...</nav>
    <div className="cork-header__actions">
      search button
      create button
      profile avatar/dropdown
    </div>
  </div>
</header>

На desktop:
- brand слева
- nav по центру
- search/+profile справа
- hamburger скрыт

На mobile:
- можно показать hamburger
- можно скрыть nav
- но hamburger не должен дублировать profile menu на desktop

Задача 3. Acid не должен зависеть от предыдущей light/dark темы

При theme === 'acid':
- html должен иметь data-theme="acid"
- html НЕ должен иметь class="dark"
- компоненты Acid должны брать цвета из CSS variables:
  --cork-bg
  --cork-surface
  --cork-surface-2
  --cork-text
  --cork-muted
  --cork-border
  --cork-frame
  --cork-brand
  --cork-king
  --cork-clown

Проверить, что после переключения:
Light → Acid
Dark → Acid

результат визуально одинаковый.

Задача 4. Старые hardcoded Tailwind цвета

Найти новые/затронутые компоненты, где есть:
bg-white
bg-gray-*
text-gray-*
dark:bg-*
dark:text-*
border-gray-*
bg-indigo-*
text-indigo-*

В тематизируемых частях заменить на cork-* классы.

Особенно проверить:
- Header
- Layout
- CreateAchievementDialog
- ChallengesPage
- ChallengeCard
- SettingsPage
- FeedPage

Задача 5. Не надо пока делать идеальный Acid на всех страницах

Минимальная цель:
- все страницы в Acid не должны выглядеть как смесь light/dark
- фон, карточки, текст, границы, кнопки должны быть управляемы cork variables
- если страница ещё не имеет Acid-декора, это нормально
- но она не должна оставаться белой/серой от старой темы

Задача 6. Проверки

После исправления:
npm run lint
npx tsc -b --noEmit
npm test
npm run build

И вручную проверить:
1. выбрать Light → перейти на Challenges → выбрать Acid → Challenges не белые
2. выбрать Dark → перейти на Challenges → выбрать Acid → результат такой же как Light → Acid
3. открыть Create Achievement dialog в Acid → не белый/серый Tailwind
4. Header на desktop: слева бренд, центр nav, справа search/+/profile
5. hamburger на desktop отсутствует
6. /feed работает без FeedPageAcid или FeedPageAcid больше не используется